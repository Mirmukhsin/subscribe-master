spring:
  config:
    activate:
      on-profile: prod

  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://postgres:5432/subscribe_master"
    username: postgres
    password: 9779

  data:
    redis:
      host: redis
      port: 6379
